<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@drawable/gradient_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Text styles -->
    <style name="TitleTextStyle">
        <item name="android:textSize">28sp</item>
        <item name="android:textColor">@color/textPrimary</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:letterSpacing">0.02</item>

        <item name="android:shadowColor">#000000</item>  <!-- semi-transparent black -->
        <item name="android:shadowDx">2</item>             <!-- horizontal offset -->
        <item name="android:shadowDy">2</item>             <!-- vertical offset -->
        <item name="android:shadowRadius">4</item>
    </style>

    <style name="DescriptionTextStyle">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/textSecondary</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:lineSpacingMultiplier">1.2</item>
        <item name="android:shadowColor">#000000</item>  <!-- semi-transparent black -->
        <item name="android:shadowDx">2</item>             <!-- horizontal offset -->
        <item name="android:shadowDy">2</item>             <!-- vertical offset -->
        <item name="android:shadowRadius">4</item>
    </style>

    <!-- Button style -->
    <style name="GetStartedButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textColor">@color/buttonText</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingStart">32dp</item>
        <item name="android:paddingEnd">32dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:elevation">4dp</item>
        <item name="android:shadowColor">#000000</item>  <!-- semi-transparent black -->
        <item name="android:shadowDx">2</item>             <!-- horizontal offset -->
        <item name="android:shadowDy">2</item>             <!-- vertical offset -->
        <item name="android:shadowRadius">4</item>
    </style>
</resources>
